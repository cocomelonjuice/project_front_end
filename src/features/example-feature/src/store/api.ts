/**
 * Example Feature API
 * Similar to portal feature API pattern
 */

// Example API functions
// import axios from 'axios';

const api = {
  // Example:
  // getExampleData: (data: { id: string }) =>
  //   axios.get(`/api/example/${data.id}`),
};

export default api;


